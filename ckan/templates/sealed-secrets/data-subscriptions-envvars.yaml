{{- if and .Values.general.useSealedSecrets (and .Values.dataSubscriptions.enable .Values.dataSubscriptions.env) }}
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  annotations:
    sealedsecrets.bitnami.com/cluster-wide: "true"
  name: "{{ .Values.general.projectId }}-data-subscriptions-envvars"
spec:
  {{- with .Values.dataSubscriptions.env }}
  encryptedData:
    {{ toYaml . | nindent 4 }}
  {{- end }}
  template:
    metadata:
      name: "{{ .Values.general.projectId }}-data-subscriptions-envvars"
    type: Opaque
{{- end }}