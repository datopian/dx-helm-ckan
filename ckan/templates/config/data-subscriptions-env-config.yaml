{{- if and .Values.dataSubscriptions.enable .Values.dataSubscriptions.config }}
{{- range $key, $value := .Values.dataSubscriptions.config }}
  {{- if or (regexMatch "(?i)postgres(ql)?://" (toString $value)) (regexMatch "(?i)redis://" (toString $value)) (regexMatch "(?i)secret" $key) }}
    {{- fail (printf "\n\n[SECURITY ERROR]: Detected sensitive data in '.Values.dataSubscriptions.config.%s'!\n" $key) }}
  {{- end }}
{{- end }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Values.general.projectId }}-data-subscriptions-env-config"
data:
  {{- with .Values.dataSubscriptions.config }}
  {{ toYaml . | nindent 2 }}
  {{- end }}
{{- end }}