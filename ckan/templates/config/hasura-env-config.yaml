{{- if and .Values.hasura.enable .Values.hasura.config }}
{{- range $key, $value := .Values.hasura.config }}
  {{- if or (regexMatch "(?i)postgres(ql)?://" (toString $value)) (regexMatch "(?i)redis://" (toString $value)) (regexMatch "(?i)secret" $key) }}
    {{- fail (printf "\n\n[SECURITY ERROR]: Detected sensitive data in '.Values.hasura.config.%s'!\n" $key) }}
  {{- end }}
{{- end }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Values.general.projectId }}-hasura-env-config"
data:
  {{- with .Values.hasura.config }}
  {{ toYaml . | nindent 2 }}
  {{- end }}
{{- end }}