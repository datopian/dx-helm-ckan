{{- if and .Values.dataAPI.enable .Values.dataAPI.config }}
{{- range $key, $value := .Values.dataAPI.config }}
  {{- if or (regexMatch "(?i)postgres(ql)?://" (toString $value)) (regexMatch "(?i)redis://" (toString $value)) (regexMatch "(?i)secret" $key) }}
    {{- fail (printf "\n\n[SECURITY ERROR]: Detected sensitive data in '.Values.dataAPI.config.%s'!\n" $key) }}
  {{- end }}
{{- end }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Values.general.projectId }}-data-api-env-config"
data:
  {{- with .Values.dataAPI.config }}
  {{ toYaml . | nindent 2 }}
  {{- end }}
{{- end }}