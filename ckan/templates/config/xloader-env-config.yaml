{{- if .Values.xloader.config }}
{{- range $key, $value := .Values.xloader.config }}
  {{- if or (regexMatch "(?i)postgres(ql)?://" (toString $value)) (regexMatch "(?i)redis://" (toString $value)) (regexMatch "(?i)secret" $key) }}
    {{- fail (printf "\n\n[SECURITY ERROR]: Detected sensitive data in '.Values.xloader.config.%s'!\n" $key) }}
  {{- end }}
{{- end }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Values.general.projectId }}-xloader-env-config"
data:
  {{- with .Values.xloader.config }}
  {{ toYaml . | nindent 2 }}
  {{- end }}
{{- end }}