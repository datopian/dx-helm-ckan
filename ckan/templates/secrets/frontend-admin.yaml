{{- if .Values.frontend-admin.enable -}}
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ .Values.general.projectId }}-frontend-admin-envvars
{{- with .Values.frontend-admin.env }}
stringData:
  {{- toYaml . | nindent 2 }}
{{- end }}
{{- end }}
